/**
 * Program that remvoes duplicates from an unsorted linked list
 */

#include <stdio.h>
#include "node.h"

void remove(node *head);

int main(void)
{
    // create arrays to be turned into linked lists
    int a1[] = { 2, 7, 6, 2, 4, 2, 8, 9, 1  };
    int a1[] = { 1, 2, 4, 5, 6, 7, 8, 8, 10 };

    // print arrays
    for (int i = 0; i < 10; i++)
    {
        printf("%d ", a1[i]);
        printf("\n");
    }

    for (int i = 0; i < 10; i++)
    {
        printf("%d ", a2[i]);
        printf("\n");
    }

    // create linked lists
    
}

void remove(node *head)
{

}